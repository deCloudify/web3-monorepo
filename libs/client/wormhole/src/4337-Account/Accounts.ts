import { Client, Presets } from 'userop';
import config from './config';
import * as dotenv from 'dotenv';
dotenv.config();

export default async function creationSimpleAccount(

  ) 
{


const paymaster = 




}
